services:
  postgres:
    image: postgres:16
    container_name: chen_leveling_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: chen
      POSTGRES_PASSWORD: chen
      POSTGRES_DB: chen_leveling
    ports:
      - "${POSTGRES_PORT:-5433}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
